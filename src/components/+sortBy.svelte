<script lang="ts">
    // export let alphabetSorting: boolean;
    // export let digitSorting: boolean;

    // function handleSort(x: string) {
    //     if (x === "alpha") {

    //     } else if (x === "numeric") {

    //     }
    // }
</script>

<div class="flex justify-center">
    <!-- <div class="w-[77%] flex space-x-2">
        <button class="" on:click={() => handleSort("alpha")}
            >Name:
            {#if alphabetSorting}
                [Z-a]
            {:else}
                [A-z]
            {/if}
        </button>
        <button class="" on:click={() => handleSort("numeric")}
            >Id:
            {#if digitSorting}
                [1000-001]
            {:else}
                [001-1000]
            {/if}</button
        >
    </div> -->
</div>
